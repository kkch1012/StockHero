# ============================================
# StockHero 환경변수 설정
# 이 파일을 .env.local로 복사하여 사용하세요
# ============================================

# --------------------------------------------
# Supabase (필수)
# https://supabase.com 에서 발급
# --------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --------------------------------------------
# 사이트 URL (필수)
# --------------------------------------------
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --------------------------------------------
# 구독 시스템 (PortOne)
# https://portone.io 콘솔에서 발급
# --------------------------------------------

# 구독 기능 활성화 (true/false)
NEXT_PUBLIC_SUBSCRIPTION_ENABLED=false

# PortOne 상점 정보 (결제 UI에서 사용 - 클라이언트 노출)
NEXT_PUBLIC_PORTONE_STORE_ID=your-store-id
NEXT_PUBLIC_PORTONE_CHANNEL_KEY=your-channel-key

# PortOne API 인증 (서버에서만 사용 - 절대 노출 금지)
PORTONE_API_SECRET=your-api-secret
PORTONE_WEBHOOK_SECRET=your-webhook-secret
PORTONE_PG_MID=your-pg-mid

# 결제 테스트 모드 (개발 환경에서만 true 설정, 프로덕션에서는 절대 사용 금지)
# PAYMENT_TEST_MODE=true

# --------------------------------------------
# AI/LLM API Keys (교차검증에는 3개 모두 필요)
# --------------------------------------------

# 개별 API 키
ANTHROPIC_API_KEY=your-anthropic-key
GOOGLE_AI_API_KEY=your-google-ai-key
OPENAI_API_KEY=your-openai-key

# --------------------------------------------
# 주식 시세 API (선택)
# --------------------------------------------

# 한국투자증권 KIS API
KIS_APP_KEY=your-kis-app-key
KIS_APP_SECRET=your-kis-app-secret
KIS_ACCOUNT_NUMBER=your-account-number

# --------------------------------------------
# Vercel Cron 인증 (선택)
# --------------------------------------------
CRON_SECRET=your-cron-secret

# --------------------------------------------
# 관리자 설정 (선택)
# --------------------------------------------
ADMIN_SECRET=your-admin-secret

# --------------------------------------------
# 기타 설정
# --------------------------------------------
NODE_ENV=development

# 환경변수 검증 결과는 서버 시작 시 콘솔에서 확인 가능합니다.
# 필수 환경변수가 누락되면 프로덕션에서 서버가 시작되지 않습니다.
